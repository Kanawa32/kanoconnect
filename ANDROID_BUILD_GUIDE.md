# ============================================
# Kano Connect - Complete Android APK Build Guide
# ============================================

# 1. Dockerfile for Android APK Building
# 2. Capacitor Configuration
# 3. Local APK Building Steps
# 4. Render Deployment Notes

echo ""
echo "=== GUIDE STARTED ==="
echo ""

# ============================================
# SECTION 1: Dockerfile
# ============================================
echo ""
echo "--- Dockerfile for Android APK Build ---"
echo ""
echo "Create a file named 'Dockerfile' in your project root (C:\\Users\\musaa\\Desktop\\Kano Connect Completed\\Dockerfile)"
echo ""
echo "The Dockerfile has already been created with the following content:"
echo ""
echo "# Use Node.js 18 with Alpine for smaller image size"
echo "FROM node:18-alpine AS builder"
echo ""
echo "# Install build dependencies"
echo "RUN apk add --no-cache python3 make g++ bash git"
echo ""
echo "# Set working directory"
echo "WORKDIR /app"
echo ""
echo "# Copy package files and install dependencies"
echo "COPY frontend/package.json frontend/package-lock.json ./"
echo "RUN npm install"
echo ""
echo "# Install Capacitor CLI globally"
echo "RUN npm install -g @capacitor/cli"
echo ""
echo "# Copy all frontend source code"
echo "COPY frontend/ ."
echo ""
echo "# Build the web app (Vite/Ract build)"
echo "RUN npm run build"
echo ""
echo "# Add Android platform to Capacitor"
echo "RUN npx cap add android 2>/dev/null || true"
echo ""
echo "# Sync web assets (dist/ -> android assets)"
echo "RUN npx cap sync android"
echo ""
echo "# Build Android APK (debug mode by default)"
echo "RUN npx cap build android"
echo ""
echo "# Verify APK was built"
echo "RUN if [ -f \"app/platforms/android/app/build/outputs/apk/debug/app-debug.apk\" ]; then"
echo "    echo \"SUCCESS: APK built at app/platforms/android/app/build/outputs/apk/debug/app-debug.apk\""
echo "fi"
echo ""
echo "--- To build the APK using Docker ---"
echo "$ docker build -t kanoconnect-android ."
echo "$ docker run -v \$(pwd):/app kanaconnect-android"
echo ""
echo "--- APK Location after build ---"
echo "app/platforms/android/app/build/outputs/apk/debug/app-debug.apk"
echo ""

# ============================================
# SECTION 2: Capacitor Configuration
# ============================================
echo ""
echo "--- Existing Capacitor Configuration ---"
echo ""
echo "Your project already has capacitor.config.json with:"
echo ""
echo "{"
echo "  \"appId\": \"com.kanoconnect.app\","
echo "  \"appName\": \"KanoConnect\","
echo "  \"webDir\": \"dist\""
echo "}"
echo ""
echo "--- Configuration Notes ---"
echo ""
echo "appId: Your Android package name (reverse domain style)"
echo "appName: Display name shown on device"
echo "webDir: Where Capacitor looks for your built web app (set to 'dist' for Vite)"
echo ""
echo "--- To update Android settings ---"
echo "1. Run: npx cap update android"
echo "2. Open android/ directory in Android Studio"
echo "3. Configure: AndroidManifest.xml, build.gradle, signing configs"
echo ""
echo "--- Required Android Permissions (add to AndroidManifest.xml) ---"
echo "<uses-permission android:name=\"android.permission.INTERNET\" />"
echo "<uses-permission android:name=\"android.permission.ACCESS_FINE_LOCATION\" />"
echo "<uses-permission android:name=\"android.permission.ACCESS_COARSE_LOCATION\" />"
echo "<uses-permission android:name=\"android.permission.ACCESS_NETWORK_STATE\" />"
echo "<uses-permission android:name=\"android.permission.READ_EXTERNAL_STORAGE\" />"
echo "<uses-permission android:name=\"android.permission.WRITE_EXTERNAL_STORAGE\" />"
echo "(Android 10+ uses Scoped Storage - consult latest Capacitor docs)"
echo ""

# ============================================
# SECTION 3: Local APK Building Steps
# ============================================
echo ""
echo "--- Building APK Locally (Without Docker) ---"
echo ""
echo "Option A: Using npm scripts (recommended)"
echo ""
echo "# 1. Install Capacitor CLI if not installed"
echo "npm install -g @capacitor/cli"
echo ""
echo "# 2. Initialize Capacitor (if not already done)"
echo "npx cap init"
echo ""
echo "# 3. Add Android platform"
echo "npx cap add android"
echo ""
echo "# 4. Build the web app first"
echo "npm run build  # or: vite build"
echo ""
echo "# 5. Sync web assets to Android"
echo "npx cap sync android"
echo ""
echo "# 6. Open Android project in Studio"
echo "npx cap open android"
echo ""
echo "# 7. In Android Studio:"
echo "   - Click Build > Build APK(s)"
echo "   - Or: Build > Build Bundle(s) / APK(s) > Build APK(s)"
echo ""
echo "Option B: Using Gradle directly"
echo ""
echo "# After cap sync, build with Gradle:"
echo "cd android"
echo "./gradlew assembleDebug"
echo ""
echo "APK will be at: android/app/build/outputs/debug/app-debug.apk"
echo ""

# ============================================
# SECTION 4: Render Deployment Notes
# ============================================
echo ""
echo "--- Render Deployment for Backend (Node.js) ---"
echo ""
echo "Your render.yaml already configures the backend service:"
echo ""
echo "services:"
echo "  - type: web"
echo "    name: kanoconnect-api"
echo "    runtime: node"
echo "    buildCommand: cd backend && npm install"
echo "    startCommand: cd backend && npm start"
echo "    healthCheckPath: /health"
echo ""
echo "This works without a Dockerfile - Render handles the Node.js setup."
echo ""
echo "--- If you want to use Docker on Render ---"
echo "1. Change render.yaml service type to 'docker'"
echo "2. Use the Dockerfile created above"
echo "3. Set buildContext to '.'"
echo "4. Expose port 10000"
echo ""
echo "--- Frontend (Static Site) on Render ---"
echo "Your render.yaml also has:"
echo "  - type: static"
echo "    name: kanoconnect"
echo "    buildCommand: cd frontend && npm install && npm run build"
echo "    staticPublishPath: frontend/dist"
echo ""
echo "This is the recommended approach - Render handles the static build."
echo ""

# ============================================
# SECTION 5: Quick Start Commands
# ============================================
echo ""
echo "--- Quick Start Commands ---"
echo ""
echo "# 1. Install Capacitor globally"
echo "npm install -g @capacitor/cli"
echo ""
echo "# 2. Initialize (first time only)"
echo "cd frontend && npx cap init KanoConnect com.kanoconnect.app"
echo ""
echo "# 3. Add Android platform"
echo "npx cap add android"
echo ""
echo "# 4. Build web app"
echo "npm run build"
echo ""
echo "# 5. Sync and build APK"
echo "npx cap sync android && npx cap build android"
echo ""
echo "# 6. Find the APK"
echo "ls -la android/app/build/outputs/apk/debug/"
echo ""

echo "=== GUIDE COMPLETE ==="